import { usePage } from '@inertiajs/react';

export const Tournament = () => {
  const user = usePage().props.auth.user;
  return (
    <div style={{backgroundColor: 'wheat', borderRadius: '20px', height: '14vh', width: '80vw'}}>
        
    </div>
  );
}